framework: jest
tier: large
preRun:
     command:
        - yarn install
        - yarn run compile
postMerge:
  pattern: 
    - "./test/**/*.test.ts"
preMerge:
  pattern:
    - "./test/**/*.test.ts"
postRun:
     command:
        - yarn --version
version: 1.0